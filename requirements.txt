# 聚宽量化策略本地部署依赖清单
# 安装方法：pip install -r requirements.txt

# ============================================================================
# 核心数据科学库
# ============================================================================
numpy>=1.24.0
pandas>=2.0.0

# ============================================================================
# 本地数据源（主要数据源，免费）
# ============================================================================
akshare>=1.14.0

# ============================================================================
# 可选数据源（更全面的数据，需要 token 或付费）
# ============================================================================

# Tushare Pro（需要申请 token，数据较全）
# tushare>=1.4.0

# JQData 本地版（聚宽官方本地数据，付费）
# jqdatasdk>=1.9.0

# ============================================================================
# 技术分析库
# ============================================================================
# TA-Lib（用于计算技术指标，如 MA / RSI / MACD）
# 安装说明：需先安装 C 库，见 https://ta-lib.org/install/
# ta-lib>=0.4.28

# 纯 Python 的 TA 替代（无需 C 库）
# pandas-ta>=0.3.14b0  # 暂时注释，因为numba不支持Python 3.14

# ============================================================================
# 可视化（可选，用于绘制回测权益曲线）
# ============================================================================
matplotlib>=3.7.0
# plotly>=5.0.0   # 交互式图表（可选）

# ============================================================================
# 其他工具
# ============================================================================
requests>=2.31.0
python-dateutil>=2.8.2
pytz>=2023.3
tqdm>=4.65.0        # 进度条
